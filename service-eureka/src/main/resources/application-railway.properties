# Cuando subas a Railway, esta variable será la URL que te dé Railway
eureka.instance.hostname=${EUREKA_HOST}
eureka.client.service-url.default-zone=http://${eureka.instance.hostname}:${server.port}/eureka/